 <!--nav-->
            <nav class="navbar navbar-default navbar-custom" role="navigation">
                <div class="container">
                    <div class="navbar-header">
                        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                            <span class="sr-only">Toggle navigation</span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                        <a class="navbar-brand" href="index.html"><%= image_tag asset_path('logo.png'),:height=>"40" %></a>
                    </div>
                    <div class="collapse navbar-collapse">
                        <ul class="nav navbar-nav navbar-right">
                            <li><a class="nav-link" href="#">Features</a></li>
                            <li><a class="nav-link" href="#">Stories</a></li>
                            <li><a class="nav-link" href="#">Pricing</a></li>
                             <% if current_user %>
                                <li id="user_header">  
                                   <span style="pull-left" ><%= display_user_image(current_user, "large") %> &nbsp;<span>
                                   <span style="pull-right;float:right"> 
                                     <%= link_to (current_user.first_name || current_user.email), user_path(current_user) %>
                                   </span>
                                </li>
                                <li>  <%= link_to "Log Out", logout_path, :class => "nav-link"  %></li>
                              <% else %>
                                <li> <%= link_to "Sign Up", plans_path, :class => "nav-link"  %> </li>
                                <li> <%#= link_to "Sign in", login_path, :class => "nav-link", "data-toggle"=>"modal", "data-target"=>"#myModal" %>  
                                    <a class="nav-link" href="#" data-toggle="modal" data-target="#myModal">Sign in</a>
                                </li>

                              <% end %>
                            <li><a class="nav-link" href="#" id="get-started">Get Started</a></li>
                        </ul>
                    </div>
                </div>
            </nav>

      <div Style="margin:10px;padding-top:30px;font-weight:600;">
      <% flash.each do |name, msg| %>
          <%= content_tag :div, msg, id: "flash_#{name}" %>
      <% end %>
    </div>
    <div id="myModal" class="modal fade" role="dialog">
      <div class="modal-dialog">
        <!-- Modal content-->
        <div class="modal-content">
          <div class="modal-header">
            <button type="button" class="close" data-dismiss="modal">&times;</button>
            <h4 class="modal-title">Sign in</h4>
          </div>
          <div class="modal-body login-form">
                 <%= form_tag sessions_path,:id => "signin_form" do %>
                <fieldset>
                    <div class="form-group">
                      <%= text_field_tag :email,params[:email], :class=>"form-control", :placeholder=>"E-mail" %>
                  </div>
                  <div class="form-group">
                    <%= password_field_tag :password,"",:class=>"form-control", :placeholder=>"Password" %>
                  </div>
                  <button class="btn btn-lg btn-success " type="submit" onclick="$('.signin_form').submit();">Login</button>
                  <%= submit_tag "Login",:class => "btn btn-lg btn-success",:style => "display:none;" %>
                    <span class="pull-right forgot">
                          <%= link_to "forgot password?", new_password_reset_path,:class => "" %>
                    </span>
                </fieldset>
                  <% end %>      
            </div>
        </div>
      </div>
    </div>